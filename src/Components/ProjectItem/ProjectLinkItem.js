import React from "react";
import { Container } from "../Containers";
import { Link } from "../TextElements";

export default function ProjectLinkItem(props) {
    const { project } = props;
    return (
        <Container place="center">
            <Link>link</Link>
        </Container>
    );
}